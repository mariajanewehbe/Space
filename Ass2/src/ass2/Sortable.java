package ass2;

public interface Sortable
{
	public int compareTo(Object o);
	/*	a.compareTo(b) returns 0 if a = b, a number greater 
	than 0 if a is greater than b and a number less than 0 
	if a is less than b. See the notes below for what it means for a to be greater than b.*/
}

